import React from "react";
import * as Styled from "../globalStyles";

function Header() {

    return (
        <Styled.Header>
            Fairly App
        </Styled.Header>
    );
}

export default Header;